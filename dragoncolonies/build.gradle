archivesBaseName = 'dragoncolonies'

dependencies {
    implementation fg.deobf(fileTree(dir: rootProject.file('libs'), include: '*bookofdragons*.jar'))
    implementation fg.deobf(fileTree(dir: rootProject.file('libs'), include: '*minecolonies*.jar'))
    implementation fg.deobf(fileTree(dir: rootProject.file('libs'), include: '*structurize*.jar'))
    implementation fg.deobf(fileTree(dir: rootProject.file('libs'), include: '*blockui*.jar'))
    implementation fg.deobf(fileTree(dir: rootProject.file('libs'), include: '*azurelib*.jar'))
}